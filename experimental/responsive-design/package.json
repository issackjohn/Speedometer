{
    "name": "responsive-design",
    "version": "1.0.0",
    "description": "A responsive design workload that uses matchMedia API for performance testing.",
    "engines": {
        "node": ">=18.13.0",
        "npm": ">=8.19.3"
    },
    "private": true,
    "scripts": {
        "dev": "http-server ./ -p 7003 -c-1 --cors",
        "build": "node scripts/build.js",
        "serve": "http-server ./dist -p 7004 -c-1 --cors"
    },
    "devDependencies": {
        "http-server": "^14.1.1"
    }
}